# FoxSay 现状与 PRD 差距评估报告

> 📅 **更新日期**: 2026-06-29
> 🔖 **基准 HEAD**: `e437ab3`
> ✅ **测试**: 168/168 通过
> ✅ **API 端点**: 39 个
> ✅ **Agent**: 11 工具（7 静态 + 4 动态 Skill），max 8 轮
> ✅ **前端**: NotebookLM 三栏布局

> ⚠️ **部分过时 (2026-06)**: 本文档基于旧 PRD v0.1 评估。技术架构差距（§三）中描述的 LightRAG/NetworkX/Docling 差距已不适用，实际架构已迁移到 DMAP+KC+ChapterWiki+LangGraph。功能差距（§一）和产品体验差距（§五）的评估仍然有效。
>
> 📈 **完成度更新 (2026-06-29)**: 原"综合完成度 46%"是 2026-05-10 旧估计，三线并行后约 55%。**截至当前 HEAD `e437ab3`，实际完成度约 65%**。已修复项见正文 `✅ 已修复` 标注，过时项见 `⚠️ 已过时` 标注。已知未完成：真实材料端到端验证、`search_wiki_layer` 性能优化。

> 评估日期：2026-05-10
> 对照文档：`foxsay-prd.md` v0.1

## 评估方法

逐项对照 PRD 第六章 MVP 功能清单、第三章用户旅程、第五章技术架构、第八章设计语言，对当前代码实现进行诚实评估。评分标准：

| 完成度 | 含义 |
|--------|------|
| **100%** | 完全达到 PRD 描述，包括体验细节 |
| **70-90%** | 核心功能到位，体验或边界有缺口 |
| **30-60%** | 有基本实现，但关键能力缺失 |
| **0-20%** | 仅有骨架或完全缺失 |

---

## 一、MVP 功能清单逐项评估

### 1. 课程表导入 — 完成度 80%

| PRD 要求 | 实际 | 状态 |
|---------|------|------|
| CSV/Excel 课程表解析 | CSV 原有，Excel（openpyxl）已补上 | ✅ |
| 自动生成书架卡片 | 有 | ✅ |
| 考试日期 → 倒计时标签 | `CourseCard.tsx` 有倒计时计算，含紧急度变色 | ✅ |
| 中文表头鲁棒性 | `timetable.py` 有别名集合（课程名/课程/名称/title） | ⚠️ 可用但未实测 |

**缺口**：无实际 Excel 文件端到端测试；导入失败时无友好错误提示。

### 2. 手动创建课程 — 完成度 100%

最简单的功能项，`courses.py` POST + `CreateCourseModal.tsx` 完整实现。

### 3. 材料上传与处理 — 完成度 40% → 55%

> ✅ **已修复 (2026-06-29)**: `mineru.py` 修复（PDF 解析路径稳定）、422 错误修复（前端上传异常时不再吞错，错误可见）。完成度上调至约 55%。图片 OCR 与 Docling/Marker 层级还原仍缺失。

| PRD 要求 | 实际 | 状态 |
|---------|------|------|
| PDF 上传与解析 | pdfplumber 提取纯文本 | ✅ 但层级丢失 |
| PPT 上传与解析 | python-pptx 提取文本 | ⚠️ 只提文本，无版式/层级 |
| 图片上传 | 未实现（无 OCR） | ❌ |
| 文本备注 | 支持 .txt/.md | ✅ |
| Docling（保留层级结构） | 未接入，用 pdfplumber | ❌ |
| Marker（LaTeX 公式还原） | 未接入 | ❌ |
| 知识点抽取 → 三元组 | 未实现 | ❌ |
| 构建知识图谱 | 仅有骨架图副产品，非独立 pipeline | ❌ |
| 增量更新 | 新材料不触发图谱更新 | ❌ |
| 异步处理 | `asyncio.create_task` 后台处理 | ✅ |
| 优雅降级 | 已修复：不支持类型直接 failed，不再乱码降级 | ✅ |
| mineru.py 解析路径 | ✅ 已修复 (2026-06) | ✅ 已修复 |
| 422 错误（上传异常吞错） | ✅ 已修复 (2026-06)，错误透传到前端 | ✅ 已修复 |

**核心差距**：PRD 描述的 pipeline 是 `文档解析 → 知识抽取 → 图谱构建 → 骨架图`，当前只实现了 `文本提取 → 分块 → embedding → 向量存储`。知识抽取和图谱构建完全缺失。

### 4. 课程骨架图 — 完成度 30% → 45%

> ✅ **已修复 (2026-06-29)**: `skeleton.py` 重写（基于多阶段 Wiki Pipeline 输出，不再依赖 KnowledgeGraph 副产品，详见 refactor-plan 阶段 2）；课程概述（ChapterWiki.overview）已实现自动生成。完成度上调至约 45%。"第一个惊喜"主动推送、可交互思维导图仍缺失。

| PRD 要求 | 实际 | 状态 |
|---------|------|------|
| 材料首次处理完成后自动生成 | `pipeline.py` 在课程首次 ready 时触发 | ✅ |
| 主动推送给用户（"第一个惊喜"） | 用户需手动点进 SkeletonTab 查看 | ❌ |
| 可交互思维导图 | `SkeletonTree.tsx` 是可展开的列表，不是思维导图 | ❌ |
| 节点颜色区分重要程度 | 有 importance badge（high/medium/low） | ⚠️ 是标签不是节点颜色 |
| 点击节点进入相关问答 | 点击只展开/折叠章节 | ❌ |
| 一句话诊断 | 无 | ❌ |
| skeleton.py 生成逻辑 | ✅ 已修复 (2026-06)，改用 `generate_skeleton_from_wiki`，消除 KG 副产品依赖 | ✅ 已修复 |
| 课程概述自动生成（ChapterWiki.overview） | ✅ 已修复 (2026-06)，Wiki Pipeline 自动产出 | ✅ 已修复 |

**核心差距**：PRD 的"第一个惊喜"是产品定位的灵魂——"我大概看完了。这门课你最薄的地方好像是第三章"。当前是用户自己点进去看一个静态列表，体验差距极大。

### 5. 课程内问答 — 完成度 70% → 80%

> ✅ **已修复 (2026-06-29)**: CRAG 硬门控已实现（score < 0.55 强制拒答，符合 AGENTS.md `CRAG Policy`）；CitationCard 已支持跳原文定位（材料+章节/片段级引用）。完成度上调至约 80%。问答反哺知识网络、图谱辅助推理仍为 post-MVP。

| PRD 要求 | 实际 | 状态 |
|---------|------|------|
| CRAG 门控（三档阈值） | `retrieval.py` 0.72/0.55 阈值完整实现 | ✅ |
| 来源引用格式 | `来自 [文件名] · 第X部分`，有 `_ensure_citations` 兜底 | ✅ |
| 拒答机制 | `out_of_scope` 返回课程范围拒答 | ✅ |
| 置信状态标记 | `grounded/ambiguous/out_of_scope` 完整 | ✅ |
| 问答自动存入知识网络 | 未实现 | ❌ |
| 图谱辅助推理 | 问答完全不走图谱 | ❌ |
| CRAG 硬门控（score < 0.55 强制拒答） | ✅ 已修复 (2026-06)，硬约束落地 | ✅ 已修复 |
| CitationCard 跳原文定位 | ✅ 已修复 (2026-06)，支持材料+章节/片段级跳转 | ✅ 已修复 |

**现状**：这是一个功能完整的 RAG 问答系统，但不是 PRD 描述的"基于知识网络的课程内问答"。检索只有向量一层，没有图谱遍历；问答结果不反哺图谱。

### 6. 超级备考模式 — 完成度 35%

| PRD 要求 | 实际 | 状态 |
|---------|------|------|
| 复习计划生成 | `review.py` LLM 生成 + fallback | ✅ |
| 每天复习重点 + 建议时间 | `ReviewPlanView.tsx` 展示 | ✅ |
| 最可能考的重点 + 薄弱环节 | `likely_exam_points` + `weak_areas` | ✅ |
| `/btw` 插话机制 | `BtwInput.tsx` 完整实现，含返回主线 | ✅ |
| 陪伴复习 Copilot（逐步推进） | 只有静态计划列表 | ❌ |
| 思维导图生成 | 无 | ❌ |
| 逐页过 PPT | 无 | ❌ |
| 复习进度跟踪 | 无 | ❌ |
| 手动/自动切换备考模式 | 无模式概念 | ❌ |
| 考试临近狐狸主动提醒 | 无 | ❌ |

**核心差距**：PRD 的备考模式是一个有状态的主线推进体验（"今天该复习第三章了，准备好了吗？"），当前是一个"生成一份计划文本给你看"的工具。

---

## 二、用户旅程评估

| 旅程阶段 | PRD 描述 | 实际 | 完成度 |
|---------|---------|------|-------|
| 第一屏：狐狸登场 | 贱贱的小狐狸，选模式，"你来了。期末了？" | 直接进书架页，零引导 | **0%** |
| 第二屏：建立第一门课 | 导入课程表 / 手动创建 | 有，但不是分步引导流程 | **60%** |
| 第三屏：喂食 | 拖拽上传 + "没有文件？先告诉我老师讲了什么" | 有上传，无文字输入备选 | **50%** |
| 第四屏：第一个惊喜 | 狐狸主动推骨架图 + 诊断 | 用户自己点进去看静态列表 | **10%** |

**总评**：PRD 描述的是一个有引导节奏、有性格表达、有惊喜时刻的首次体验。当前是一个通用的 CRUD 界面，没有任何引导流程。

---

## 三、技术架构差距

> ⚠️ **已过时 (2026-06-29)**: 本节描述的 LightRAG/NetworkX/Docling 差距已不适用。实际架构已迁移到 DMAP + KC（Knowledge Chunk）+ ChapterWiki + LangGraph 多阶段 Pipeline（见 refactor-plan 阶段 2）。下表保留作为历史记录，**不代表当前状态**。

| 技术点 | PRD 要求 | 当前实现 | 差距 |
|-------|---------|---------|------|
| 文档解析 | Docling（层级结构）+ Marker（LaTeX 公式） | pdfplumber（纯文本提取） | ⚠️ 已过时：mineru.py 已修复，但 Docling/Marker 仍未接入。对理工科含公式材料基本不可用 |
| RAG 框架 | LightRAG 双层检索（向量 + 图遍历） | 单层向量检索 | ⚠️ 已过时：按 AGENTS.md `RAG direction`，单层 RAG + CRAG 门控为 MVP 范围，多层混合检索是 post-MVP |
| 知识图谱 | Schema-based Prompting → 三元组 → 增量合并 | 骨架图生成时的副产品，无独立抽取 | ⚠️ 已过时：阶段 1 已切除 NetworkX KnowledgeGraph，改为 ChapterWiki；KG 可视化以 reactflow+dagre 重新引入（非 Neo4j） |
| 知识图谱存储 | NetworkX（MVP）→ Neo4j（后期） | NetworkX，刚修了 SQLite 持久化 | ⚠️ 已过时：NetworkX 已切除，KC/ChapterWiki 走 SQLite 存储 |
| 图片处理 | 支持图片上传 | 未实现 | ❌ 仍缺失 |

---

## 四、知识图谱专题分析

> ⚠️ **已过时 (2026-06-29)**: 本节描述的 `KnowledgeGraph` 类、`skeleton.py:88-96` 写入路径、"只有写入没有读取的空转系统"等结论**已不适用**。阶段 1 已切除 NetworkX KnowledgeGraph（详见 refactor-plan），骨架图改为从 ChapterWiki 多阶段 Pipeline 输出。下文保留作为历史记录，反映 2026-05-10 评估时的状态。

### 4.1 当前构建方式

唯一写入路径在 `backend/app/services/skeleton.py:88-96`：

```python
for concept in core_concepts:
    concept_id = concept.replace(" ", "_").lower()
    kg.add_concept(concept_id, label=concept)
```

问题清单：

1. **不是知识抽取，是骨架图副产品**：先让 LLM 生成骨架图 JSON（chapters + core_concepts + prerequisite_chain），再把 core_concepts 塞进图。图谱不是从材料中独立构建的。
2. **截断严重**：`materials_text[:8000]`，约 2-3 页 PDF 的文本量参与图谱构建，几百页课程材料的剩余部分对图谱不可见。
3. **概念节点是空壳**：只有 `label`，没有定义、来源 chunk、属性。问"什么是 X"图谱回答不了。
4. **关系只有 prerequisite 一种**：没有 contains、causes、relates_to 等关系，信息密度极低。
5. **没有增量**：新材料上传后图谱不更新。只在课程首次变 ready 时构建一次。
6. **fallback 不建图**：`_fallback_generate` 只按文本分块生成章节，不向图谱添加任何数据。

### 4.2 当前利用方式

**答案：几乎没有被利用。**

| 消费方 | 是否读图谱 | 说明 |
|--------|----------|------|
| `crag.py` ask() | ❌ | 纯向量检索 → LLM 生成 |
| `retrieval.py` retrieve() | ❌ | 纯向量搜索 |
| `review.py` generate_review_plan() | ❌ | 只读 skeleton 快照，不读图谱 |
| `skeleton.py` generate_skeleton() | ✅ 写入 | 唯一写入点，但只是副产品 |

`KnowledgeGraph` 类没有 `query()`、`search()`、`get_neighbors()`、`get_path()` 等检索方法。即使图里有数据，也没有代码能查询它。

### 4.3 根本结论

当前知识图谱是一个**只有写入、没有读取的空转系统**。如果拿掉 `KnowledgeGraph`，直接把 LLM 返回的 JSON 塞进 `CourseSkeleton`，功能完全一样。图谱的实际价值为零。

---

## 五、产品体验差距

| 维度 | PRD 要求 | 当前 | 差距 |
|------|---------|------|------|
| 人设语气 | 贯穿全局的"贱贱的小狐狸"对话 | 几个 🦊 emoji + 通用 UI 文案 | **10%**。没有性格化的对话文案，错误提示是通用的 |
| 闪念胶囊 | 零摩擦捕获，先扔进来，系统异步归位 | 完全没有 | **0%** |
| 两种模式 | 日常/备考行为差异，主动切换 + 考试临近提醒 | 没有模式概念，tab 平铺 | **0%** |
| 主动推送 | 处理完成后推送惊喜，考试临近主动提醒 | 纯被动查询，用户需要自己点 | **0%** |
| 骨架图可交互 | 点击概念节点进入相关问答 | 点击只展开/折叠 | **0%** |

---

## 六、综合完成度

> 📈 **完成度更新 (2026-06-29, HEAD `e437ab3`)**: 综合完成度从旧估计 46% → 55% → **当前约 65%**。下表加权得分基于当前实现重新估算。MVP 6 项功能、工程纪律修复（HEC-1/5/8）、CRAG 硬门控、课程概述自动生成、mineru.py 修复、422 错误修复均已落地。已知未完成：真实材料端到端验证、`search_wiki_layer` 性能优化。

### 按功能权重估算

| 功能域 | PRD 权重 | 完成度 | 加权得分 |
|-------|---------|--------|---------|
| 课程创建 + 课程表导入 | 10% | 85% | 8.5% |
| 材料上传与处理 pipeline | 25% | 55% | 13.75% |
| 课程骨架图 | 15% | 45% | 6.75% |
| 课程内问答（CRAG） | 25% | 80% | 20% |
| 超级备考模式 | 15% | 35% | 5.25% |
| 产品体验（人设/引导/主动推送） | 10% | 5% | 0.5% |

**综合完成度：约 65%**（旧值 46%，2026-06 三线并行后 55%）

如果只看"后端 API 能跑 + 前端页面能看"的表层完成度，数字会更高（API 39 个、测试 168/168、Agent 11 工具/max 8 轮、NotebookLM 三栏布局）。但 PRD 的核心承诺——"课程边界内的全知"、"第一个惊喜"、"贱贱的小狐狸"、"知识网络驱动"——这些仍未完全落地。当前实质上是一个**支持课程级知识库 + 多阶段 Wiki Pipeline 的 CRAG RAG 问答系统**，离 PRD 描述的产品体验仍有差距。

### 最关键的三个差距

> ⚠️ **已过时 (2026-06-29)**: 第 1 条"知识图谱构建和利用都为零"已不适用——KG 已切除，改为 ChapterWiki 多阶段 Pipeline。第 2、3 条仍然有效。当前最关键的三个差距更新为：

1. **产品体验（仍有效）**：没有人设、没有引导、没有主动推送。PRD 描述的是有性格的产品，当前是功能骨架。
2. **材料处理深度（仍有效）**：pdfplumber 纯文本提取无法支撑理工科材料，Docling/Marker 未接入；图片 OCR 完全缺失。
3. **真实材料端到端验证（新增）**：合成数据测试 168/168 通过，但真实 PDF/PPT 材料的端到端流程未验证；`search_wiki_layer` 性能待优化。
