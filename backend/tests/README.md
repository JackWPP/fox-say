# Tests Placeholder

Future tests should first protect project constraints:
- course-scoped schemas include `course_id`
- CRAG refuses below threshold
- non-refusal answers include citations
- no route performs model-only fallback when retrieval fails
- post-MVP features are not introduced accidentally

